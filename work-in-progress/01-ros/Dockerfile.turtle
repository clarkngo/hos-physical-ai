FROM ros:jazzy-ros-base

# Install turtlesim
RUN apt-get update && apt-get install -y \
    ros-jazzy-turtlesim \
    && rm -rf /var/lib/apt/lists/*

# We'll run the turtle node manually so we can interact with it
CMD ["bash"]